export interface ReaderBootstrapHighlight {
  range: Range;
  selectedHtml: string;
  selectedText: string;
  comment: string;
}

export interface ExternalHighlightPayload {
  range: Range;
  selectedHtml: string;
  selectedText: string;
  comment: string;
}
