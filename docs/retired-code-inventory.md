# Retired Code Inventory

Last updated: 2026-05-31

## Decision Rules

- `delete-now`: path is not in the production build graph, has no runtime import, and tests are either removed or migrated.
- `migrate-then-delete`: path currently has test, script, or internal callers that must move to Stitch/domain code first.
- `retain-production`: path participates in production build or is the current domain owner.
- `retain-test-fixture`: path is not production but is an explicit fixture outside `src`.
- Non-Production Code 3.0 source deletion requires all six exact-path owner proofs to be present and empty: production build graph, retained source import/re-export graph, tests/visual/browser checks, package/build scripts, public/manifest assets, and required verification commands.
- `npm run audit:non-production-source:report` is inventory evidence and must exit 0 in a completed orchestration. Record its counts and exit status; a non-zero report exit means exact blocking rows still require migration, six-proof deletion, or explicit retained-contract classification.
- `npm run audit:non-production-source:check` is the hard gate; it must exit 0 and fail closed on unclassified stop rows or unsafe `delete-now` proof contradictions.
- `npm run audit:retired-code:report` now fails when any `delete-now` path exists under `src` or is referenced by package scripts, build scripts, `src`, tests, public/manifest assets, or required visual/browser verification.
- Retired Options compatibility classes and the retired preview runtime must not be reintroduced. Any retained `migrate-then-delete` family below must keep its owner/reason wording until production build graph, import graph, script/public ownership, and test/visual ownership all prove it can move to `delete-now`.
- 2026-05-22 review gap truth: M6.2 low-reuse retained-code retirement was a safe no-op. The six-proof table found no additional `delete-approved` retained low-reuse source paths, and the M6.2 ledger records no source deletion. Treat the remaining retained and `migrate-then-delete` rows below as follow-up debt, not as completed retirement.
- 2026-05-26 M10 non-production source truth: `audit:non-production-source:report` exits 0 with decision counts `migrate-import-owner: 200`, `retain-production: 527`, and `retain-production-facade: 15`; these counts are inventory evidence, not deletion approval.

| Path Family                                                               | Decision            | Runtime Owner                                            | Verification Owner                                          | Evidence Command                                                                                                                                      | Replacement Owner                                                   | Required Action                                                                                                                                                     | Delete Gate                                                                                                                                                                     |
| ------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/options/preview/**`                                                  | delete-now          | none in production build graph                           | none after migration to `tests/fixtures/options-preview/**` | `npm run audit:build-graph:report`; `rg -n "options/preview" package.json scripts src tests public`                                                   | `tests/fixtures/options-preview/**`                                 | deleted from `src` in the retired-code removal milestone                                                                                                            | no package script, build script, test, or visual spec imports `src/options/preview`; `verify:stitch-secondary` is green without it                                              |
| `src/options/components/formSections/**`                                  | migrate-then-delete | none in production build graph                           | source-of-truth doc blocker remains                         | `npm run audit:build-graph:report`; `npm run audit:deps:report`; import scan                                                                          | Stitch schema/settings builders                                     | owner: `src/options/README.md` still documents the compatibility layer; tests have moved to production Stitch coverage, so update docs and re-audit before deletion | no production, doc, or test imports remain; build, typecheck, audit gates green                                                                                                 |
| `src/options/components/layout/**`                                        | delete-now          | none after exact source deletion                         | none after source-of-truth docs and audit tools moved       | `npm run audit:production-build-graph:report`; exact import scan; `npm run audit:options-mainline:report`; `npm run audit:ui-architecture:report`     | production Stitch shell/sidebar render path                         | deleted in Plan 05 layout-only retirement with six-proof evidence under `.tmp/aiiinob-yaml-options-retirement-2026-05-31/options-layout-form/ledger.md`             | all six owner proof categories empty; production Stitch shell and audit gates green                                                                                             |
| old class sections under `src/options/components/sections/*Section.ts`    | migrate-then-delete | none in production build graph                           | retained REST helper and `BaseSection` blockers             | `npm run audit:build-graph:report`; import scan                                                                                                       | Stitch schema builders and production shell                         | non-REST class sections deleted in Plan 06; remaining REST/BaseSection compatibility is isolated to Plan 07                                                         | no non-REST imports remain; REST/BaseSection deletion waits for Plan 07                                                                                                         |
| `src/options/components/sections/usageChart.ts`                           | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/components/sections/usageChartRenderers.ts`                  | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/components/sections/usageChartTypes.ts`                      | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/components/sections/usageDashboard.utils.ts`                 | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| `src/options/components/sections/usageDashboardLayout.ts`                 | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| `src/options/components/sections/usageDashboardState.ts`                  | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| REST helper files under `src/options/components/sections/restSection*.ts` | retain-production   | current REST section/view helpers                        | REST section tests and Options/Stitch verification          | `npm run audit:build-graph:report`; `npm run verify:stitch-secondary`                                                                                 | production Stitch storage surface                                   | classify per file during REST/Stitch migration                                                                                                                      | production shell and REST tests green                                                                                                                                           |
| `src/options/components/sections/restSectionConnectionResult.ts`          | delete-now          | none after import owners moved to current REST owner     | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; REST connection-result import scan; `npm run audit:retired-code:report`                                | `src/options/app/rest-settings/restSectionConnectionResult.ts`      | deleted in M1.3 REST shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-rest-shim-retirement/`                     | all six owner proof categories empty; current owner tests import `@options/app/rest-settings/restSectionConnectionResult`                                                       |
| `src/options/widgets/YamlConfigWidget.ts`                                 | retain-production   | production shell mounts `yaml-config` widget             | production shell and YAML widget tests                      | `npm run audit:build-graph:report`; production shell tests                                                                                            | new split files under `src/options/widgets/yaml-config/`            | split in hotspot milestone, then keep adapter until no longer mounted                                                                                               | production shell tests green                                                                                                                                                    |
| non-YAML `src/options/widgets/**`                                         | migrate-then-delete | none after production host imports YAML adapter directly | exact six-proof delete candidates                           | `npm run audit:production-build-graph:report`; `npm run audit:non-production-source:report`; `npm run audit:non-production-source:check`; import scan | Stitch native controls or `src/ui/domains/*`                        | all non-YAML widget adapters are eligible for source deletion only when each exact path has all six owner proofs empty                                              | no `@options/widgets` production imports remain except explicitly retained YAML adapter; exact paths must be `delete-now` and the safety check must pass before source deletion |
| `src/options/widgets/shared/usage/usageChart.ts`                          | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/widgets/shared/usage/usageChartRenderers.ts`                 | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/widgets/shared/usage/usageChartTypes.ts`                     | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/ui/domains/usage-chart/**`                                     | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@ui/domains/usage-chart`                                                                                      |
| `src/options/widgets/shared/usage/usageDashboard.utils.ts`                | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| `src/options/widgets/shared/usage/usageDashboardLayout.ts`                | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| `src/options/widgets/shared/usage/usageDashboardState.ts`                 | delete-now          | none in production build graph                           | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; old usage import scan; `npm run audit:retired-code:report`                                             | `src/options/app/usage-dashboard/**`                                | deleted in M1.2 usage shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-usage-shim-retirement/`                   | all six owner proof categories empty; current owner tests import `@options/app/usage-dashboard`                                                                                 |
| `src/options/widgets/shared/rest/restSectionConnectionResult.ts`          | delete-now          | none after import owners moved to current REST owner     | none after compatibility test migration                     | `npm run audit:production-build-graph:report`; REST connection-result import scan; `npm run audit:retired-code:report`                                | `src/options/app/rest-settings/restSectionConnectionResult.ts`      | deleted in M1.3 REST shim retirement with six-proof evidence under `.tmp/aiiinob-deep-debt-2026-05-24/retained-source/m1-rest-shim-retirement/`                     | all six owner proof categories empty; current owner tests import `@options/app/rest-settings/restSectionConnectionResult`                                                       |
| `src/ui/domains/video/SupportPromptView.ts`                               | delete-now          | none in production build graph                           | none found by build graph owner scan                        | `npm run audit:build-graph:report`; import scan                                                                                                       | `src/ui/domains/support-prompt/SupportPromptView.ts`                | deleted from `src` in the retired-code removal milestone                                                                                                            | import scan empty; deletion commit verifies content/video tests                                                                                                                 |
| `src/options/stitch/schema/settings/experimental.ts`                      | delete-now          | none in production build graph                           | none found by build graph owner scan                        | `npm run audit:build-graph:report`; schema registry import scan                                                                                       | no replacement or current schema owner                              | deleted from `src` in the retired-code removal milestone                                                                                                            | import scan empty and production shell tests green                                                                                                                              |
| `src/infrastructure/optionsRepository.ts`                                 | delete-now          | none in production build graph                           | none after Plan 08 barrel/test/doc migration                | `npm run audit:build-graph:report`; `npm run audit:repository-composition:report`; import scan                                                        | `IOptionsRepository`, `ChromeOptionsRepository`, typed repositories | deleted in Plan 08 infrastructure OptionsRepository retirement; legacy infrastructure adapter exports and docs were removed                                         | repository audit green; no imports, barrel exports, or compatibility tests remain                                                                                               |
